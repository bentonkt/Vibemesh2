# JAX-port requirements — install into brain_env on the 3090 only
# Do NOT install on shitter (WSL) to avoid breaking the running CPU experiments.
#
# Install command (3090, PowerShell):
#   C:\Users\bento\brain_env\Scripts\pip.exe install -r scripts/jax/requirements-jax.txt
#
# Note: jax[cuda12] requires CUDA 12.x. The 3090 should already have it.

jax[cuda12]>=0.4.25
mujoco-mjx>=3.7.0     # must match installed mujoco version (3090 has 3.7.0)
sbx-rl>=0.10.0        # Stable-Baselines3 JAX — for future training (Phase 5)
flax>=0.8.0           # sbx dependency
optax>=0.2.0          # sbx dependency
